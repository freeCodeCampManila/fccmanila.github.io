{
  "name": "fccmanila.github.io",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url='./'",
    "deploy": "yarn build && gh-pages -d dist --branch master"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel": "^1.12.4"
  }
}
